/*
 * server.h - Header file for server functionality
 * 
 * INSTRUCTIONS:
 * 1. Add header guards (#ifndef SERVER_H, #define SERVER_H, #endif)
 * 2. Define a Server struct that contains:
 *    - socket_fd (int) - the server socket file descriptor
 *    - port (int) - the port number
 *    - is_running (int) - flag to indicate if server is running (0 or 1)
 * 3. Declare function prototypes:
 *    - int server_init(int port)
 *    - int server_start(void)
 *    - void server_stop(void)
 *    - void handle_client(int client_fd)
 * 
 * LEARNING GOALS:
 * - Understand header guards and why they're needed
 * - Learn about struct definitions
 * - Practice declaring function prototypes
 * - Understand the separation of interface (.h) and implementation (.c)
 */

// TODO: Add header guards

// TODO: Define Server struct

// TODO: Declare function prototypes

// TODO: Close header guard

